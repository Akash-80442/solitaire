services:
  - type: web
    name: trick-royale-server
    env: node
    rootDir: server
    buildCommand: yarn install
    startCommand: yarn start
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 18.x
